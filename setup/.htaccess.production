# SECURITY: Zabezpečení setup adresáře - PRODUCTION
# Toto je produkční verze - KOMPLETNĚ BLOKOVAT přístup

# Apache 2.4+
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

# Apache 2.2
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>

# Extra bezpečnost - zakázat spouštění PHP
<Files *.php>
    Require all denied
</Files>

# Poznámka: V produkci zkopírovat tento soubor jako setup/.htaccess
# cp setup/.htaccess.production setup/.htaccess

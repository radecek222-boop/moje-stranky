# SECURITY: Blokovat webovy pristup ke scriptum
# Tyto skripty jsou urceny pouze pro CLI nebo localhost

<IfModule mod_authz_core.c>
    # Apache 2.4+
    Require all denied
</IfModule>

<IfModule !mod_authz_core.c>
    # Apache 2.2
    Order deny,allow
    Deny from all
</IfModule>

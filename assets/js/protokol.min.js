// Kontrola - tato stránka je pouze pro techniky a adminy(async function(){try{const response = await fetch("app/admin_session_check.php");const data = await response.json();if (!data.logged_in){alert("Přihlaste se prosím");window.location.href = "login.php";return}if (data.role === "prodejce"){alert("Tato stránka je pouze pro techniky a administrátory");window.location.href = "seznam.php"}}catch (err){logger.error("Chyba kontroly přístupu:",err)}})();// === HAMBURGER MENU ===function toggleMenu(){const navMenu = document.getElementById('navMenu');const hamburger = document.querySelector('.hamburger');navMenu.classList.toggle('active');hamburger.classList.toggle('active')}// Zavřít menu při kliknutí na odkazdocument.addEventListener('DOMContentLoaded',() =>{const navLinks = document.querySelectorAll('.nav a');navLinks.forEach(link =>{link.addEventListener('click',() =>{const nav = document.getElementById('navMenu');const hamburger = document.querySelector('.hamburger');nav.classList.remove('active');hamburger.classList.remove('active')})})});let signaturePad;let attachedPhotos = [];let currentReklamaceId = null;let currentReklamace = null;window.addEventListener("DOMContentLoaded",async () =>{logger.log('🚀 Inicializace protokolu...');initSignaturePad();const urlParams = new URLSearchParams(window.location.search);currentReklamaceId = urlParams.get('id');logger.log('📋 ID z URL:',currentReklamaceId);if (currentReklamaceId){logger.log('✅ ID nalezeno v URL');await loadReklamace(currentReklamaceId);loadPhotosFromDatabase(currentReklamaceId)}else{logger.warn('⚠️ Chybí ID v URL - zkusím načíst z localStorage');await loadReklamace(null);if (currentReklamace && currentReklamace.id){logger.log('✅ ID nalezeno v načtených datech:',currentReklamace.id);currentReklamaceId = currentReklamace.id;loadPhotosFromDatabase(currentReklamaceId)}else{logger.error('❌ ID se nepodařilo najít!')}}const today = new Date().toISOString().split('T')[0];document.getElementById("sign-date").value = today;document.getElementById("visit-date").value = today;setupAutoTranslate()});function setupAutoTranslate(){const fields = ['description','problem','repair'];fields.forEach(field =>{const czField = document.getElementById(field+'-cz');let timeout;czField.addEventListener('input',() =>{clearTimeout(timeout);timeout = setTimeout(() =>{if (czField.value.trim().length>5){translateField(field,true)}},1500)})})}function initSignaturePad(){const canvas = document.getElementById("signature-pad");const resize = () =>{const ratio = Math.max(window.devicePixelRatio || 1,1);canvas.width = canvas.offsetWidth * ratio;canvas.height = canvas.offsetHeight * ratio;canvas.getContext("2d").scale(ratio,ratio)};window.addEventListener("resize",resize);resize();signaturePad = new SignaturePad(canvas,{minWidth:1,maxWidth:2.5,penColor:"black",backgroundColor:"white"})}async function loadPhotosFromDatabase(customerId){try{if (!customerId){logger.warn('ID zákazníka nenalezeno');return}logger.log('═══════════════════════════════════════');logger.log('🖼️ NAČÍTÁM FOTKY Z DATABÁZE');logger.log('═══════════════════════════════════════');logger.log('🔑 customerId:',customerId);// Načíst z API const response = await fetch(`api/get_photos_api.php?reklamace_id=${customerId}`);const data = await response.json();if (!data.success || data.total_photos === 0){logger.log('❌ Fotky nenalezeny v databázi');showNotif("warning","Nebyly nalezeny fotky");logger.log('═══════════════════════════════════════');return}logger.log('✅ Fotky načteny z databáze!');const sections = data.sections;logger.log('📦 Sekce:',Object.keys(sections));const sectionLabels ={'before':'BEFORE','id':'ID','problem':'DETAIL BUG','repair':'REPAIR','after':'AFTER'};let totalPhotos = 0;let totalVideos = 0;const orderedSections = ['before','id','problem','repair','after'];orderedSections.forEach(sectionKey =>{const sectionItems = sections[sectionKey];if (!Array.isArray(sectionItems) || sectionItems.length === 0) return;logger.log(`📁 Sekce "${sectionKey}":${sectionItems.length}položek`);sectionItems.forEach(item =>{if (item.type === 'video'){totalVideos++}else if (item.type === 'image' || !item.type){if (item.data){attachedPhotos.push({data:item.data,label:sectionLabels[sectionKey] || sectionKey.toUpperCase(),section:sectionKey});totalPhotos++}}})});logger.log(`📊 CELKEM:${totalPhotos}fotek,${totalVideos}videí`);if (attachedPhotos.length>0){const previewPhotos = attachedPhotos.map(p =>typeof p === 'string' ? p:p.data);renderPhotoPreview(previewPhotos);showNotif("success",`✓ Načteno ${totalPhotos}fotek`);logger.log('✅ Fotky úspěšně načteny s popisky')}else{logger.log('⚠️ Žádné fotky k zobrazení');showNotif("info","Žádné fotky")}logger.log('═══════════════════════════════════════')}catch (error){logger.error('❌ Chyba při načítání fotek:',error);showNotif("error","Chyba načítání fotek")}}async function loadReklamace(id){showLoading(true);try{logger.log('🔍 Načítám data zákazníka...');logger.log('📋 ID z URL:',id);const localData = localStorage.getItem('currentCustomer');if (localData){logger.log('✅ Data nalezena v localStorage');const customer = JSON.parse(localData);logger.log('📦 Data zákazníka:',customer);// ✅ KONTROLA OPRÁVNĚNÍ const currentUser = JSON.parse(localStorage.getItem('currentUser') || '{}');logger.log('👤 Aktuální uživatel:',currentUser.name,'| Role:',currentUser.role);if (currentUser.role === 'prodejce'){// Prodejce může vidět jen své zakázky if (customer.zpracoval_id && customer.zpracoval_id !== currentUser.id){showNotif('error','Nemáte oprávnění k této zakázce');setTimeout(() =>window.location.href = 'seznam.php',2000);showLoading(false);return}}// Admin a technik vidí všechny zakázky - bez kontroly logger.log('✅ Oprávnění potvrzeno');const customerName = customer.jmeno || customer.zakaznik || '';let ulice = '',mesto = '',psc = '';if (customer.adresa){const parts = customer.adresa.split(',').map(s =>s.trim());ulice = parts[0] || '';mesto = parts[1] || '';psc = parts[2] || '';logger.log('📍 Adresa (nový formát):',{ulice,mesto,psc})}else{ulice = customer.ulice || '';mesto = customer.mesto || '';psc = customer.psc || '';logger.log('📍 Adresa (starý formát):',{ulice,mesto,psc})}logger.log('📝 Vyplňuji formulář...');document.getElementById("order-number").value = customer.id || customer.cislo || "";document.getElementById("claim-number").value = customer.id || customer.reklamace_id || customer.cislo || "";document.getElementById("customer").value = customerName;document.getElementById("address").value = customer.adresa || `${ulice},${mesto},${psc}`;document.getElementById("phone").value = customer.telefon || "";document.getElementById("email").value = customer.email || "";document.getElementById("brand").value = customer.znacka || customer.model || "";document.getElementById("model").value = customer.model || "";document.getElementById("description-cz").value = customer.popis_problemu || "";currentReklamace = customer;currentReklamaceId = customer.id || customer.cislo;logger.log('✅ Data zákazníka úspěšně načtena a vyplněna');showNotif("success","✓ Data načtena");showLoading(false);return}logger.warn('⚠️ Data v localStorage nenalezena');if (!id){showNotif("error","Chybí ID reklamace");showLoading(false);return}const response = await fetch(`api/protokol_api.php?action=load_reklamace&id=${id}`);const result = await response.json();if (result.status === 'success'){logger.log('✅ Data načtena z API');currentReklamace = result.reklamace;const customerName = currentReklamace.jmeno || currentReklamace.zakaznik || '';let ulice = '',mesto = '',psc = '';if (currentReklamace.adresa){const parts = currentReklamace.adresa.split(',').map(s =>s.trim());ulice = parts[0] || '';mesto = parts[1] || '';psc = parts[2] || ''}else{ulice = currentReklamace.ulice || '';mesto = currentReklamace.mesto || '';psc = currentReklamace.psc || ''}document.getElementById("order-number").value = currentReklamace.id || currentReklamace.cislo || "";document.getElementById("claim-number").value = currentReklamace.id || currentReklamace.reklamace_id || "";document.getElementById("customer").value = customerName;document.getElementById("address").value = currentReklamace.adresa || `${ulice},${mesto},${psc}`;document.getElementById("phone").value = currentReklamace.telefon || "";document.getElementById("email").value = currentReklamace.email || "";document.getElementById("brand").value = currentReklamace.znacka || currentReklamace.model || "";document.getElementById("model").value = currentReklamace.model || "";document.getElementById("description-cz").value = currentReklamace.popis_problemu || "";showNotif("success","Reklamace načtena")}else{showNotif("error","Reklamace nenalezena")}}catch (error){logger.error('❌ Chyba načítání:',error);showNotif("error","Chyba načítání")}finally{showLoading(false)}}function showLoading(show){document.getElementById("loadingOverlay").classList.toggle("show",show)}function showNotif(type,message){const notif = document.getElementById("notif");notif.className = `notif ${type}`;notif.textContent = message;notif.classList.add("show");setTimeout(() =>notif.classList.remove("show"),3000)}function updateTotal(){const work = parseFloat(document.getElementById("price-work").value) || 0;const material = parseFloat(document.getElementById("price-material").value) || 0;const second = parseFloat(document.getElementById("price-second").value) || 0;const transport = parseFloat(document.getElementById("price-transport").value) || 0;const total = work+material+second+transport;document.getElementById("price-total").value = total.toFixed(2)+" Kč"}async function attachPhotos(){const input = document.createElement("input");input.type = "file";input.accept = "image/*";input.multiple = true;input.capture = "environment";input.style.display = "none";document.body.appendChild(input);input.onchange = async (e) =>{const files = Array.from(e.target.files || []);if (!files.length) return;showNotif("success","Zpracovávám fotky...");for (const file of files){const compressed = await compressImage(file,0.6);const base64 = await toBase64(compressed);attachedPhotos.push(base64)}renderPhotoPreview(attachedPhotos);showNotif("success",`${files.length}fotek přidáno`);input.remove()};input.click()}async function compressImage(file,maxMB = 0.6){const img = await loadImage(URL.createObjectURL(file));const canvas = document.createElement("canvas");const ctx = canvas.getContext("2d");const maxW = 1200;const s = Math.min(1,maxW / img.width);canvas.width = img.width * s;canvas.height = img.height * s;ctx.drawImage(img,0,0,canvas.width,canvas.height);let q = 0.85;let blob = await new Promise(r =>canvas.toBlob(r,"image/jpeg",q));while (blob.size>maxMB * 1024 * 1024 && q>0.4){q -= 0.05;blob = await new Promise(r =>canvas.toBlob(r,"image/jpeg",q))}return blob}function loadImage(src){return new Promise((r,j) =>{const i = new Image();i.onload = () =>r(i);i.onerror = j;i.src = src})}function toBase64(blob){return new Promise((r,j) =>{const fr = new FileReader();fr.onload = () =>r(fr.result);fr.onerror = j;fr.readAsDataURL(blob)})}function renderPhotoPreview(arr){let cont = document.getElementById("photoPreviewContainer");if (!cont){cont = document.createElement("div");cont.id = "photoPreviewContainer";document.querySelector(".wrapper").appendChild(cont)}cont.innerHTML = `<h3>Připojené fotky (${arr.length})</h3><div id="photoGrid"></div>`;const grid = cont.querySelector("#photoGrid");arr.forEach(src =>{const photoData = typeof src === 'string' ? src:src.data;const img = document.createElement("img");img.src = photoData;img.onclick = () =>window.open(photoData,"_blank");grid.appendChild(img)})}async function generateProtocolPDF(){const{jsPDF}= window.jspdf;const doc = new jsPDF("p","mm","a4");doc.setLanguage('cs');// Podpora češtiny doc.setLanguage('cs');// Podpora češtiny const wrapper = document.querySelector(".wrapper");const btns = document.querySelector(".btns");btns.style.display = "none";const canvas = await html2canvas(wrapper,{scale:3,backgroundColor:"#fff",useCORS:true,logging:false,imageTimeout:0,allowTaint:true,letterRendering:true});const imgData = canvas.toDataURL("image/jpeg",0.98);const pageWidth = 210;const pageHeight = 297;const margin = 10;const availableWidth = pageWidth - (margin * 2);const availableHeight = pageHeight - (margin * 2);const canvasRatio = canvas.height / canvas.width;let imgWidth = availableWidth;let imgHeight = imgWidth * canvasRatio;if (imgHeight>availableHeight){imgHeight = availableHeight;imgWidth = imgHeight / canvasRatio}const xOffset = (pageWidth - imgWidth) / 2;const yOffset = margin;doc.addImage(imgData,"JPEG",xOffset,yOffset,imgWidth,imgHeight);btns.style.display = "flex";return doc}async function generatePhotosPDF(){if (!attachedPhotos.length) return null;const{jsPDF}= window.jspdf;const pdf = new jsPDF("p","mm","a4");doc.setLanguage('cs');// Podpora češtiny doc.setLanguage('cs');// Podpora češtiny const pageWidth = pdf.internal.pageSize.getWidth();const pageHeight = pdf.internal.pageSize.getHeight();const margin = 10;const gap = 5;const labelHeight = 5;// Těsnější mezera pro text const photosPerPage = 4;const cols = 2;const rows = 2;const availableWidth = pageWidth - (2 * margin) - gap;const availableHeight = pageHeight - (2 * margin) - gap;const cellWidth = availableWidth / cols;const cellHeight = availableHeight / rows;logger.log(`📄 Vytvářím PDF:${attachedPhotos.length}fotek,${Math.ceil(attachedPhotos.length / photosPerPage)}stránek`);for (let i = 0;i < attachedPhotos.length;i++){const photo = attachedPhotos[i];const photoData = typeof photo === 'string' ? photo:photo.data;const photoLabel = typeof photo === 'object' ? photo.label:'';if (i>0 && i % photosPerPage === 0){pdf.addPage();logger.log(`📄 Přidána nová stránka (fotka ${i+1})`)}const indexOnPage = i % photosPerPage;const col = indexOnPage % cols;const row = Math.floor(indexOnPage / cols);const x = margin+(col * (cellWidth+gap));const y = margin+(row * (cellHeight+gap));// Text VŽDY nahoře vlevo na horní hraně fotky if (photoLabel){pdf.setFontSize(8);pdf.setFont('helvetica','bold');pdf.setTextColor(0,0,0);// Text přesně na horní hraně buňky,vlevo pdf.text(photoLabel,x+1,y+3)}const photoY = y+labelHeight;// Fotka těsně pod textem const maxPhotoWidth = cellWidth;const maxPhotoHeight = cellHeight - labelHeight;try{// Detekce velikosti obrázku pro zachování aspect ratio const img = new Image();img.src = photoData;await new Promise((resolve) =>{img.onload = resolve;// Fallback pokud se obrázek nenačte setTimeout(resolve,100)});let imgWidth = img.width || 1000;let imgHeight = img.height || 1000;// Vypočítat aspect ratio const imgRatio = imgWidth / imgHeight;const cellRatio = maxPhotoWidth / maxPhotoHeight;let finalWidth,finalHeight;// Zachovat orientaci a aspect ratio if (imgRatio>cellRatio){// Širší obrázek - omezit šířkou finalWidth = maxPhotoWidth;finalHeight = maxPhotoWidth / imgRatio}else{// Vyšší obrázek - omezit výškou finalHeight = maxPhotoHeight;finalWidth = maxPhotoHeight * imgRatio}// Vycentrovat fotku v buňce const offsetX = (maxPhotoWidth - finalWidth) / 2;// Text přímo nad fotkou if (photoLabel){doc.setFontSize(8);doc.setFont('helvetica','bold');doc.setTextColor(0,0,0);doc.text(photoLabel,x+offsetX+1,y+3)}const offsetY = (maxPhotoHeight - finalHeight) / 2;// Komprese pro menší PDF (kvalita 0.7 = dobrý kompromis) pdf.addImage(photoData,"JPEG",x+offsetX,photoY+offsetY,finalWidth,finalHeight,undefined,'MEDIUM');logger.log(` 📸 Fotka ${i+1}/${attachedPhotos.length}- ${photoLabel || 'bez popisku'}(${imgWidth}x${imgHeight}→ ${Math.round(finalWidth)}x${Math.round(finalHeight)}mm)`)}catch (err){// Fallback - použít celou buňku logger.warn(`⚠️ Nelze detekovat velikost fotky ${i+1},používám celou buňku`);pdf.addImage(photoData,"JPEG",x,photoY,maxPhotoWidth,maxPhotoHeight,undefined,'MEDIUM')}}logger.log(`✅ PDF s fotkami vytvořeno (${attachedPhotos.length}fotek s popisky)`);return pdf}async function exportBothPDFs(){try{showLoading(true);logger.log('📋 Generuji kompletní PDF (protokol+fotodokumentace)...');// Vytvořit JEDNO PDF s protokolem const doc = await generateProtocolPDF();// Pokud jsou fotky,přidat fotodokumentaci na KONEC protokolu if (attachedPhotos.length>0){logger.log('📸 Přidávám fotodokumentaci...');const pageWidth = doc.internal.pageSize.getWidth();const pageHeight = doc.internal.pageSize.getHeight();const margin = 10;// ==================================== // NOVÁ STRÁNKA:Fotodokumentace začíná // ==================================== doc.addPage();// Hlavička fotodokumentace doc.setFontSize(16);doc.setFont('helvetica','bold');doc.text('FOTODOKUMENTACE',pageWidth / 2,20,{align:'center'});let yPos = 35;// Informace o zákazníkovi doc.setFontSize(10);doc.setFont('helvetica','normal');const customerInfo = [ `Cislo reklamace:${document.getElementById('claim-number')?.value || 'N/A'}`,`Datum:${document.getElementById('datum_navstevy')?.value || new Date().toLocaleDateString('cs-CZ')}` ] customerInfo.forEach(line =>{doc.text(line,margin,yPos);yPos+= 6});yPos+= 5;// Čára doc.setLineWidth(0.5);doc.line(margin,yPos,pageWidth - margin,yPos);yPos+= 10;// Nadpis indexu doc.setFontSize(12);doc.setFont('helvetica','bold');doc.text('INDEX PHOTO',margin,yPos);yPos+= 8;// Index fotek - miniaturní náhledy doc.setFontSize(8);doc.setFont('helvetica','normal');const thumbSize = 25;const thumbGap = 5;const thumbsPerRow = Math.floor((pageWidth - 2 * margin) / (thumbSize+thumbGap));for (let i = 0;i < attachedPhotos.length;i++){const photo = attachedPhotos[i];const photoData = typeof photo === 'string' ? photo:photo.data;const photoLabel = typeof photo === 'object' ? photo.label:`Fotka ${i+1}`;const col = i % thumbsPerRow;const row = Math.floor(i / thumbsPerRow);const x = margin+(col * (thumbSize+thumbGap));const y = yPos+(row * (thumbSize+thumbGap+4));// Zkontrolovat jestli se vejde na stránku if (y+thumbSize>pageHeight - margin){doc.addPage();yPos = 20;continue}// Miniaturní náhled try{doc.addImage(photoData,"JPEG",x,y,thumbSize,thumbSize,undefined,'FAST');// Popisek pod miniaturou doc.setFontSize(7);doc.text(`${i+1}. ${photoLabel}`,x,y+thumbSize+3,{maxWidth:thumbSize})}catch (err){logger.warn(`⚠️ Nelze přidat miniaturu ${i+1}`)}}logger.log(`✅ Index ${attachedPhotos.length}fotek vytvořen`);// ==================================== // DALŠÍ STRÁNKY:Velké fotky 4 na stránku // ==================================== doc.addPage();const gap = 5;const labelHeight = 5;const photosPerPage = 4;const cols = 2;const rows = 2;const availableWidth = pageWidth - (2 * margin) - gap;const availableHeight = pageHeight - (2 * margin) - gap;const cellWidth = availableWidth / cols;const cellHeight = availableHeight / rows;for (let i = 0;i < attachedPhotos.length;i++){const photo = attachedPhotos[i];const photoData = typeof photo === 'string' ? photo:photo.data;const photoLabel = typeof photo === 'object' ? photo.label:'';if (i>0 && i % photosPerPage === 0){doc.addPage()}const indexOnPage = i % photosPerPage;const col = indexOnPage % cols;const row = Math.floor(indexOnPage / cols);const x = margin+(col * (cellWidth+gap));const y = margin+(row * (cellHeight+gap));// Text VŽDY nahoře vlevo if (photoLabel){doc.setFontSize(8);doc.setFont('helvetica','bold');doc.setTextColor(0,0,0);doc.text(photoLabel,x+1,y+3)}const photoY = y+labelHeight;const maxPhotoWidth = cellWidth;const maxPhotoHeight = cellHeight - labelHeight;try{const img = new Image();img.src = photoData;await new Promise((resolve) =>{img.onload = resolve;setTimeout(resolve,100)});let imgWidth = img.width || 1000;let imgHeight = img.height || 1000;const imgRatio = imgWidth / imgHeight;const cellRatio = maxPhotoWidth / maxPhotoHeight;let finalWidth,finalHeight;if (imgRatio>cellRatio){finalWidth = maxPhotoWidth;finalHeight = maxPhotoWidth / imgRatio}else{finalHeight = maxPhotoHeight;finalWidth = maxPhotoHeight * imgRatio}const offsetX = (maxPhotoWidth - finalWidth) / 2;// Text přímo nad fotkou if (photoLabel){doc.setFontSize(8);doc.setFont('helvetica','bold');doc.setTextColor(0,0,0);doc.text(photoLabel,x+offsetX+1,y+3)}const offsetY = (maxPhotoHeight - finalHeight) / 2;doc.addImage(photoData,"JPEG",x+offsetX,photoY+offsetY,finalWidth,finalHeight,undefined,'MEDIUM');logger.log(` 📸 Fotka ${i+1}/${attachedPhotos.length}- ${photoLabel}`)}catch (err){logger.warn(`⚠️ Chyba fotky ${i+1}`);doc.addImage(photoData,"JPEG",x,photoY,maxPhotoWidth,maxPhotoHeight,undefined,'MEDIUM')}}logger.log(`✅ Fotodokumentace přidána (${attachedPhotos.length}fotek)`);showNotif("success",`✓ PDF vytvořeno (protokol+${attachedPhotos.length}fotek)`)}else{showNotif("success","✓ Protokol vytvořen (bez fotek)")}// Otevřít JEDNO PDF window.open(URL.createObjectURL(doc.output("blob")),"_blank");// Uložit do DB await saveProtokolToDB();// Označit jako hotovou logger.log('📋 Označuji reklamaci jako hotovou...');try{const markResponse = await fetch('app/save.php',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({action:'update',id:currentReklamaceId,mark_as_completed:'1'})});const markResult = await markResponse.json();if (markResult.status === 'success'){logger.log('✅ Reklamace označena jako hotová')}}catch (err){logger.error('❌ Chyba při označování:',err)}}catch (error){logger.error('❌ Chyba při generování PDF:',error);showNotif("error","Chyba při vytváření PDF")}finally{showLoading(false)}}async function generatePhotosDocumentationPDF(){if (!attachedPhotos.length) return null;const{jsPDF}= window.jspdf;const pdf = new jsPDF("p","mm","a4");doc.setLanguage('cs');// Podpora češtiny doc.setLanguage('cs');// Podpora češtiny const pageWidth = pdf.internal.pageSize.getWidth();const pageHeight = pdf.internal.pageSize.getHeight();const margin = 10;logger.log('📄 Vytvářím fotodokumentaci...');// ============================================ // STRÁNKA 1:Hlavička zákazníka+Index fotek // ============================================ // Hlavička pdf.setFontSize(16);pdf.setFont('helvetica','bold');pdf.text('FOTODOKUMENTACE',pageWidth / 2,20,{align:'center'});let yPos = 35;// Informace o zákazníkovi pdf.setFontSize(10);pdf.setFont('helvetica','normal');const customerInfo = [ `Číslo objednávky:${document.getElementById('claim-number')?.value || 'N/A'}`,`Číslo reklamace:${document.getElementById('claim-number')?.value || 'N/A'}`,`Zákazník:${document.getElementById('zakaznik')?.value || 'N/A'}`,`Adresa:${document.getElementById('adresa')?.value || 'N/A'}`,`Technik:${document.getElementById('technik')?.value || 'N/A'}`,`Datum:${document.getElementById('datum_navstevy')?.value || new Date().toLocaleDateString('cs-CZ')}` ];customerInfo.forEach(line =>{pdf.text(line,margin,yPos);yPos+= 6});yPos+= 5;// Čára pdf.setLineWidth(0.5);pdf.line(margin,yPos,pageWidth - margin,yPos);yPos+= 10;// Nadpis indexu pdf.setFontSize(12);pdf.setFont('helvetica','bold');pdf.text('INDEX PHOTO',margin,yPos);yPos+= 8;// Index fotek - miniaturní náhledy pdf.setFontSize(8);pdf.setFont('helvetica','normal');const thumbSize = 25;const thumbGap = 5;const thumbsPerRow = Math.floor((pageWidth - 2 * margin) / (thumbSize+thumbGap));for (let i = 0;i < attachedPhotos.length;i++){const photo = attachedPhotos[i];const photoData = typeof photo === 'string' ? photo:photo.data;const photoLabel = typeof photo === 'object' ? photo.label:`Fotka ${i+1}`;const col = i % thumbsPerRow;const row = Math.floor(i / thumbsPerRow);const x = margin+(col * (thumbSize+thumbGap));const y = yPos+(row * (thumbSize+thumbGap+4));// Zkontrolovat jestli se vejde na stránku if (y+thumbSize>pageHeight - margin){pdf.addPage();yPos = 20;continue}// Miniaturní náhled try{pdf.addImage(photoData,"JPEG",x,y,thumbSize,thumbSize,undefined,'FAST');// Popisek pod miniatuůrou pdf.setFontSize(7);pdf.text(`${i+1}. ${photoLabel}`,x,y+thumbSize+3,{maxWidth:thumbSize})}catch (err){logger.warn(`⚠️ Nelze přidat miniaturu ${i+1}`)}}logger.log(`✅ Index ${attachedPhotos.length}fotek vytvořen`);// ============================================ // DALŠÍ STRÁNKY:Velké fotky 4 na stránku // ============================================ pdf.addPage();const gap = 5;const labelHeight = 5;const photosPerPage = 4;const cols = 2;const rows = 2;const availableWidth = pageWidth - (2 * margin) - gap;const availableHeight = pageHeight - (2 * margin) - gap;const cellWidth = availableWidth / cols;const cellHeight = availableHeight / rows;for (let i = 0;i < attachedPhotos.length;i++){const photo = attachedPhotos[i];const photoData = typeof photo === 'string' ? photo:photo.data;const photoLabel = typeof photo === 'object' ? photo.label:'';if (i>0 && i % photosPerPage === 0){pdf.addPage()}const indexOnPage = i % photosPerPage;const col = indexOnPage % cols;const row = Math.floor(indexOnPage / cols);const x = margin+(col * (cellWidth+gap));const y = margin+(row * (cellHeight+gap));// Text VŽDY nahoře vlevo if (photoLabel){pdf.setFontSize(8);pdf.setFont('helvetica','bold');pdf.setTextColor(0,0,0);pdf.text(photoLabel,x+1,y+3)}const photoY = y+labelHeight;const maxPhotoWidth = cellWidth;const maxPhotoHeight = cellHeight - labelHeight;try{const img = new Image();img.src = photoData;await new Promise((resolve) =>{img.onload = resolve;setTimeout(resolve,100)});let imgWidth = img.width || 1000;let imgHeight = img.height || 1000;const imgRatio = imgWidth / imgHeight;const cellRatio = maxPhotoWidth / maxPhotoHeight;let finalWidth,finalHeight;if (imgRatio>cellRatio){finalWidth = maxPhotoWidth;finalHeight = maxPhotoWidth / imgRatio}else{finalHeight = maxPhotoHeight;finalWidth = maxPhotoHeight * imgRatio}const offsetX = (maxPhotoWidth - finalWidth) / 2;// Text přímo nad fotkou if (photoLabel){doc.setFontSize(8);doc.setFont('helvetica','bold');doc.setTextColor(0,0,0);doc.text(photoLabel,x+offsetX+1,y+3)}const offsetY = (maxPhotoHeight - finalHeight) / 2;pdf.addImage(photoData,"JPEG",x+offsetX,photoY+offsetY,finalWidth,finalHeight,undefined,'MEDIUM');logger.log(` 📸 Fotka ${i+1}/${attachedPhotos.length}- ${photoLabel}`)}catch (err){logger.warn(`⚠️ Chyba fotky ${i+1}`);pdf.addImage(photoData,"JPEG",x,photoY,maxPhotoWidth,maxPhotoHeight,undefined,'MEDIUM')}}logger.log(`✅ Fotodokumentace vytvořena (${attachedPhotos.length}fotek)`);return pdf}async function sendToCustomer(){try{showLoading(true);showNotif("success","Odesílám email...");const protocolPdf = await generateProtocolPDF();const protocolBase64 = btoa(protocolPdf.output("arraybuffer"));let photosBase64 = null;if (attachedPhotos.length>0){logger.log(`📸 Vytvářím PDF z ${attachedPhotos.length}fotek...`);const photosPdf = await generatePhotosPDF();photosBase64 = btoa(photosPdf.output("arraybuffer"));logger.log('✅ PDF s fotkami vytvořeno')}else{logger.log('ℹ️ Žádné fotky k přiložení')}const response = await fetch("api/protokol_api.php",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"send_email",reklamace_id:currentReklamaceId,protokol_pdf:protocolBase64,photos_pdf:photosBase64})});const result = await response.json();if (result.status === 'success'){showNotif("success","✓ Email odeslán zákazníkovi");await saveProtokolToDB();logger.log('📋 Označuji reklamaci jako hotovou...');const markResponse = await fetch('app/save.php',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({action:'update',id:currentReklamaceId,mark_as_completed:'1'})});const markResult = await markResponse.json();if (markResult.status === 'success'){logger.log('✅ Reklamace označena jako hotová')}else{logger.warn('⚠️ Nepodařilo se označit jako hotovou:',markResult.message)}if (currentReklamaceId){const key = 'photoSections_'+currentReklamaceId;const pdfKey = 'photosPDF_'+currentReklamaceId;localStorage.removeItem(key);localStorage.removeItem(pdfKey);localStorage.removeItem('photosReadyForProtocol');localStorage.removeItem('photosCustomerId');logger.log('✅ Fotky a PDF vymazány z localStorage')}setTimeout(() =>{window.location.href = 'seznam.php'},2000)}else{showNotif("error",result.message || "Chyba odesílání")}}catch (error){logger.error(error);showNotif("error","Chyba odesílání:"+error.message)}finally{showLoading(false)}}async function saveProtokolToDB(){try{const response = await fetch("api/protokol_api.php",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({action:"save_protokol",reklamace_id:currentReklamaceId,problem_description:document.getElementById("problem-cz").value,repair_proposal:document.getElementById("repair-cz").value,solved:document.getElementById("solved").value,technician:document.getElementById("technician").value})});const result = await response.json();if (result.status === 'success'){logger.log("Protokol uložen do DB")}}catch (error){logger.error("Chyba ukládání:",error)}}// ================================================================// AUTOMATICKÝ PŘEKLAD PŘI OPUŠTĚNÍ POLE// ================================================================// Debounce funkce - čeká než uživatel přestane psátfunction debounce(func,wait){let timeout;return function executedFunction(...args){const later = () =>{clearTimeout(timeout);func(...args)};clearTimeout(timeout);timeout = setTimeout(later,wait)}}// Funkce pro překlad textu přes Google Translate APIasync function translateText(text,sourceLang = 'cs',targetLang = 'en'){if (!text || text.trim() === '') return '';try{const url = `https://translate.googleapis.com/translate_a/single?client=gtx&sl=${sourceLang}&tl=${targetLang}&dt=t&q=`+encodeURIComponent(text);const response = await fetch(url);const data = await response.json();if (data && data[0] && data[0][0] && data[0][0][0]){return data[0][0][0]}return ''}catch (err){logger.error('Chyba překladu:',err);return ''}}// Automatický překlad pro konkrétní poleasync function autoTranslateField(fieldId){const field = document.getElementById(fieldId);if (!field) return;const text = field.value.trim();if (!text) return;logger.log('🔄 Překládám pole:',fieldId);// Najít příslušný en-label let enLabel = field.parentElement.querySelector('.en-label');// Pokud neexistuje,najít ho jinak if (!enLabel){const container = field.closest('.input-group,.form-group,div');if (container){enLabel = container.querySelector('.en-label')}}if (!enLabel){logger.warn('En-label pro',fieldId,'nenalezen');return}// Přeložit const translated = await translateText(text,'cs','en');if (translated){enLabel.textContent = translated;logger.log('✅ Přeloženo:',fieldId,'->',translated.substring(0,50)+'...')}}// Inicializace auto-překladufunction initAutoTranslation(){const fieldsToTranslate = ['description','problem','repair'];fieldsToTranslate.forEach(fieldId =>{const field = document.getElementById(fieldId);if (!field) return;// Debounced funkce - překládá 1.5s po posledním stisku klávesy const debouncedTranslate = debounce(() =>{autoTranslateField(fieldId)},1500);// Event:uživatel píše field.addEventListener('input',debouncedTranslate);// Event:opustil pole (blur) field.addEventListener('blur',() =>{autoTranslateField(fieldId)});logger.log('✅ Auto-překlad aktivován pro:',fieldId)})}// Spustit po načtení stránkyif (document.readyState === 'loading'){document.addEventListener('DOMContentLoaded',initAutoTranslation)}else{initAutoTranslation()}logger.log('🌐 Automatický překlad aktivován');async function translateField(fieldName,silent = false){const czField = document.getElementById(fieldName+'-cz');const enField = document.getElementById(fieldName+'-en');if (!czField || !enField) return;const text = czField.value.trim();if (!text || text.length < 5) return;try{enField.value = 'Prekladam...';const response = await fetch('api/translate_api.php',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text:text,engine:'mymemory'})});const result = await response.json();if (result.status === 'success'){enField.value = result.translated;logger.log('OK:',fieldName)}else{enField.value = ''}}catch (e){logger.error('Err:',e);enField.value = ''}}window.addEventListener('load',() =>{['description','problem','repair'].forEach(f =>{const el = document.getElementById(f+'-cz');if (!el) return;let t;el.addEventListener('input',() =>{clearTimeout(t);t = setTimeout(() =>{if (el.value.trim().length>10) translateField(f,true)},2000)})});logger.log('Translate ready')});// === UNIVERSAL EVENT DELEGATION FOR REMOVED INLINE HANDLERS ===document.addEventListener('DOMContentLoaded',() =>{// Handle data-action buttons document.addEventListener('click',(e) =>{const target = e.target.closest('[data-action]');if (!target) return;const action = target.getAttribute('data-action');// Special cases if (action === 'reload'){location.reload();return}// Try to call function if it exists if (typeof window[action] === 'function'){window[action]()}});// Handle data-navigate buttons document.addEventListener('click',(e) =>{const navigate = e.target.closest('[data-navigate]')?.getAttribute('data-navigate');if (navigate){if (typeof navigateTo === 'function'){navigateTo(navigate)}else{location.href = navigate}}});// Handle data-onchange inputs document.addEventListener('change',(e) =>{const target = e.target.closest('[data-onchange]');if (!target) return;const action = target.getAttribute('data-onchange');const value = target.getAttribute('data-onchange-value') || target.value;if (typeof window[action] === 'function'){window[action](value)}})});

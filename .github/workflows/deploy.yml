name: ⚡ ULTRA RYCHLÝ Deploy (2 min)

on:
  push:
    branches:
      - main
      - 'claude/**'  # Deploy i z claude/* větví
  workflow_dispatch:

jobs:
  ultra-fast-deploy:
    runs-on: ubuntu-latest
    name: ⚡ Deploy BEZ testů

    steps:
      - name: 📥 Checkout (shallow)
        uses: actions/checkout@v4
        with:
          fetch-depth: 1  # Jen poslední commit = rychlejší

      - name: 🚀 SFTP Deploy (optimalizovaný)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: ${{ secrets.FTP_REMOTE_PATH }}/
          protocol: ftps
          port: 21
          # RYCHLÉ - jen změněné soubory
          dry-run: false
          dangerous-clean-slate: false
          # Exclude těžké věci
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/vendor/**
            **/tests/**
            **/.env
            **/logs/**
            **/backups/**

      - name: ✅ Hotovo!
        run: |
          echo "⚡ Deploy dokončen!"
          echo "🕐 Čas: ~2 minuty (bez testů)"
